server:
  # 禁用 Spring 生成404等错误信息，返回到前台
  error:
    whitelabel:
      enabled: false
  port: 8082
  tomcat:
    uri-encoding: UTF-8
  #servlet:
    #context-path: /config
spring:
  # JSON相关配置
  jackson:
    locale: zh_CN
    default-property-inclusion: non-null
  # Config Server相关配置  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/firechiang/springcloud-test-configs.git
          username: firechiang
          password: dsfksdfnksdnfsd
          # basedir: /Users/admin/code/myProjects/java/imooc/SpringCloud_Sell/config/basedir  使用本地配置文件的配置方式
# 配置服务访问说明
# /{name}-{profiles}.yml  
# /{label}/{name}-{profiles}.yml  
# name=服务名称
# profiles=环境
# label=配置文件所在git分支（当然不用git也就没有分支一说）  
# 日志级别相关      
logging:
  level:
    com.firecode.elktest.biz.dao: debug
    org.hibernate.SQL: debug
# 开启全局debug日志    
#debug: true